server:
  port: 8083

spring:
  application:
    name: servicio-verificacion-licencias

portal:
  verificacion:
    base-url: https://licencias.munihuamanga.gob.pe/verificar/

management:
  endpoints:
    web:
      exposure:
        include: health,info

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
